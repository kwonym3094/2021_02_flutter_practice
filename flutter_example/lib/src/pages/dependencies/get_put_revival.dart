import 'package:flutter/material.dart';

class GetPutRevival extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Container()),
    );
  }
}
